name: Build TWRP 11.0 for Teclast M20 (MT6797)

on:
  workflow_dispatch:

jobs:
  build-twrp:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git wget curl unzip python3 python3-pip bc lz4 xz-utils brotli cpio gperf libxml2-utils

      - name: Setup & Unpack with Magiskboot
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install twrpdtgen
          
          # 1. 安装基础工具
          sudo apt-get update
          sudo apt-get install -y cpio gzip zlib1g-dev liblz4-tool unzip
          
          # 2. 清理旧文件
          rm -f magisk.apk libmagiskboot.so magiskboot
          
          # 3. 下载 Magisk 的二进制工具
          wget https://github.com/topjohnwu/Magisk/releases/download/v26.1/Magisk-v26.1.apk -O magisk.apk
          
          # 4. 提取 x86_64 版本的 magiskboot
          unzip magisk.apk 'lib/x86_64/libmagiskboot.so'
          
          # 5. 重命名并赋予执行权限
          mv lib/x86_64/libmagiskboot.so ./magiskboot
          chmod +x ./magiskboot
          
          # 6. 执行解包，并强制忽略非零退出码 (GitHub误报)
          echo "🚀 正在解包 boot.img ..."
          ./magiskboot unpack boot.img || true
          
          # 7. 验证解包结果
          if [ -f "kernel" ] || [ -f "ramdisk.cpio" ]; then
            echo "✅ 恭喜！boot.img 解包成功！"
            ls -la | grep -E "(kernel|ramdisk|dtb)"
          else
            echo "❌ 解包失败，请检查 boot.img 是否损坏"
            exit 1
          fi

      - name: Manually Generate Device Tree for MT6797
        run: |
          # 1. 创建设备树目录 (厂商/设备名)
          DEVICE_PATH="device/teclast/m20_mt6797"
          mkdir -p $DEVICE_PATH
          
          echo "🌳 正在手动生成 TWRP 设备树配置..."

          # 2. 生成 AndroidProducts.mk (产品入口文件)
          cat > $DEVICE_PATH/AndroidProducts.mk << 'EOF'
          PRODUCT_MAKEFILES := \
              $(LOCAL_DIR)/twrp_m20_mt6797.mk
          EOF

          # 3. 生成 twrp_m20_mt6797.mk (产品编译配置文件)
          cat > $DEVICE_PATH/twrp_m20_mt6797.mk << 'EOF'
          $(call inherit-product, $(SRC_TARGET_DIR)/product/core_64_bit.mk)
          $(call inherit-product, $(SRC_TARGET_DIR)/product/base.mk)

          PRODUCT_DEVICE := m20_mt6797
          PRODUCT_NAME := twrp_m20_mt6797
          PRODUCT_BRAND := Teclast
          PRODUCT_MODEL := Teclast M20 4G
          PRODUCT_MANUFACTURER := Teclast

          PRODUCT_PACKAGES += \
              otapreopt_script \
              cppreopts.sh \
              update_engine \
              update_verifier \
              update_engine_sideload
          EOF

          # 4. 生成 BoardConfig.mk (最核心的硬件与分区配置文件)
          cat > $DEVICE_PATH/BoardConfig.mk << 'EOF'
          DEVICE_PATH := device/teclast/m20_mt6797

          # 基础架构配置
          TARGET_ARCH := arm64
          TARGET_ARCH_VARIANT := armv8-a
          TARGET_CPU_ABI := arm64-v8a
          TARGET_CPU_ABI2 := 
          TARGET_CPU_VARIANT := generic
          TARGET_CPU_VARIANT_RUNTIME := cortex-a72

          TARGET_2ND_ARCH := arm
          TARGET_2ND_ARCH_VARIANT := armv8-a
          TARGET_2ND_CPU_ABI := armeabi-v7a
          TARGET_2ND_CPU_ABI2 := armeabi
          TARGET_2ND_CPU_VARIANT := generic
          TARGET_2ND_CPU_VARIANT_RUNTIME := cortex-a53

          # 平台配置 (MT6797 对应 Helio X20/X23/X27)
          TARGET_BOARD_PLATFORM := mt6797
          TARGET_BOOTLOADER_BOARD_NAME := mt6797
          TARGET_NO_BOOTLOADER := true

          # 内核配置
          BOARD_KERNEL_CMDLINE := bootopt=64S3,32N2,64N2
          BOARD_KERNEL_BASE := 0x40078000
          BOARD_KERNEL_PAGESIZE := 2048
          BOARD_KERNEL_OFFSET := 0x00008000
          BOARD_RAMDISK_OFFSET := 0x11a88000
          BOARD_KERNEL_TAGS_OFFSET := 0x07808000
          BOARD_MKBOOTIMG_ARGS := --kernel_offset $(BOARD_KERNEL_OFFSET) --ramdisk_offset $(BOARD_RAMDISK_OFFSET) --tags_offset $(BOARD_KERNEL_TAGS_OFFSET)

          # 分区表配置 (根据 MT6797 常见布局设定，后续可根据实际 fstab 微调)
          BOARD_BOOTIMAGE_PARTITION_SIZE := 33554432
          BOARD_RECOVERYIMAGE_PARTITION_SIZE := 33554432
          BOARD_HAS_LARGE_FILESYSTEM := true
          BOARD_SYSTEMIMAGE_PARTITION_TYPE := ext4
          BOARD_USERDATAIMAGE_FILE_SYSTEM_TYPE := ext4
          BOARD_VENDORIMAGE_FILE_SYSTEM_TYPE := ext4
          TARGET_COPY_OUT_VENDOR := vendor

          # TWRP 专属配置
          TW_THEME := portrait_hdpi
          RECOVERY_SDCARD_ON_DATA := true
          TARGET_RECOVERY_PIXEL_FORMAT := "RGBX_8888"
          TW_BRIGHTNESS_PATH := "/sys/class/leds/lcd-backlight/brightness"
          TW_MAX_BRIGHTNESS := 255
          TW_DEFAULT_BRIGHTNESS := 120
          TW_USE_TOOLBOX := true
          TW_INCLUDE_NTFS_3G := true
          TW_INCLUDE_FUSE_EXFAT := true
          TW_EXCLUDE_SUPERSU := true
          TW_INCLUDE_CRYPTO := true
          TW_EXTRA_LANGUAGES := true
          TW_SCREEN_BLANK_ON_BOOT := true
          EOF

          # 5. 生成 recovery.fstab (分区挂载表)
          mkdir -p $DEVICE_PATH/recovery/root/etc
          cat > $DEVICE_PATH/recovery/root/etc/recovery.fstab << 'EOF'
          /boot           emmc    /dev/block/by-name/boot           flags=backup=1
          /recovery       emmc    /dev/block/by-name/recovery       flags=backup=1
          /system         ext4    /dev/block/by-name/system         flags=backup=1
          /vendor         ext4    /dev/block/by-name/vendor         flags=backup=1
          /data           f2fs    /dev/block/by-name/userdata       flags=backup=1,encryptable=footer
          /cache          ext4    /dev/block/by-name/cache          flags=backup=1
          /sdcard         vfat    /dev/block/mmcblk1p1              flags=display="Micro SDcard";storage;wipeingui;removable
          /usb-otg        auto    /dev/block/sda1                   flags=display="USB-OTG";storage;wipeingui;removable
          EOF

          echo "✅ 手动设备树生成完成！目录结构如下："
          find $DEVICE_PATH -type f

      - name: Automatic fstab Repair
        run: |
          DT="device_tree"
          # 查找 fstab 文件，通常在 device_tree/ 或 device_tree/ramdisk/etc/
          FSTAB_PATH=$(find $DT -type f -name "recovery.fstab" -o -name "fstab.*" | head -n1)
          
          if [ -n "$FSTAB_PATH" ]; then
            echo "🔧 正在修复 fstab: $FSTAB_PATH"
            # 避免重复添加，先检查是否存在
            if ! grep -q "/data" "$FSTAB_PATH"; then
              cat >> "$FSTAB_PATH" << 'EOF'

          # Auto-added for Teclast M20 (MT6797)
          /system_root    ext4    /dev/block/by-name/system    flags=backup=1
          /vendor         ext4    /dev/block/by-name/vendor    flags=backup=1
          /data           f2fs    /dev/block/by-name/userdata  flags=backup=1,encryptable=footer
          /cache          ext4    /dev/block/by-name/cache     flags=backup=1
          /boot           emmc    /dev/block/by-name/boot
          /recovery       emmc    /dev/block/by-name/recovery
          EOF
              echo "✅ fstab 修复完成"
            else
              echo "ℹ️  fstab 已包含 data 分区，跳过修复"
            fi
          else
            echo "❌ 未找到 fstab 文件，请手动检查"
          fi

      - name: Sync TWRP Source & Build
        run: |
          # 1. 安装 repo 工具
          mkdir -p ~/.bin
          PATH="${HOME}/.bin:${PATH}"
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/.bin/repo
          chmod a+rx ~/.bin/repo
          
          # 2. 确保设备树目录存在
          mkdir -p twrp_build
          cd twrp_build
          
          # 3. 初始化 TWRP 仓库
          # 注意：twrp-11.0 分支可能不存在，这里做了自动降级到 twrp-12.1 的处理
          repo init -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-11.0 --depth=1 || {
            echo "⚠️ twrp-11.0 分支克隆失败，自动尝试使用 twrp-12.1...";
            repo init -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-12.1 --depth=1;
          }
          
          # 4. 同步源码 (增加 -j1 防止国内网络拉取时并发过高导致失败)
          repo sync -c -j1 --force-sync --no-clone-bundle --no-tags || echo "部分同步失败，继续尝试..."
          
          # 5. 准备编译环境
          . build/envsetup.sh
          
          # 6. 指定设备并编译
          # 使用我们之前手动生成的设备代号 m20_mt6797
          lunch twrp_m20_mt6797-eng
          mka recoveryimage -j2

      - name: Upload TWRP Image
        uses: actions/upload-artifact@v4
        with:
          name: TWRP-11.0-Teclast-M20-MT6797
          path: twrp_build/out/target/product/*/recovery.img
          if-no-files-found: warn
